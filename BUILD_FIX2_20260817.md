# Build/Test Fix 2 — 2026-08-17

Correcciones detectadas al ejecutar la suite dentro de la imagen OCI real en RHEL 9.7/Podman:

1. Se agrega `httpx==0.28.1` y sus dependencias fijadas (`httpcore==1.0.9`, `sniffio==1.3.1`) al lock para que `fastapi.testclient.TestClient` funcione dentro de la imagen.
2. `test_bloquea_depi_local_sin_montaje` fuerza `SATYS_REQUIRE_SHARED_MOUNT=1` localmente para probar el comportamiento seguro por defecto sin heredar el `SATYS_REQUIRE_SHARED_MOUNT=0` que usa intencionalmente el runtime portable con `/shared` montado desde el host.

No cambia el comportamiento productivo de sincronización ni las rutas montadas del contenedor.
